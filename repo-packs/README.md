# repo-packs

This directory is kept in sync with [Smech-Labs/spk](https://github.com/Smech-Labs/spk),
where `spk`'s canonical source now lives as its own dedicated repository.
Make changes there; this copy exists so the build orchestrator
(`bin/build_smechos.sh`) has the source available locally without an extra
clone step.
